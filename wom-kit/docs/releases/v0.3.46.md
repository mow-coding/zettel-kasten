# v0.3.46 - IMAP Mailbox Operation Request Package

Date: 2026-06-15

v0.3.46 adds the read-only request package that sits just before any future IMAP
mailbox adapter.

## Added

- `archive imap-mailbox-operation-request-plan --dry-run`
- Alias: `archive imap-mailbox-request-plan --dry-run`
- Alias: `archive mailbox-operation-request-plan --dry-run`
- MCP tool: `imap_mailbox_operation_request_plan`
- Public doc:
  [IMAP Mailbox Operation Request Plan](https://github.com/mow-coding/zettel-kasten/blob/main/wom-kit/docs/imap-mailbox-operation-request-plan.md)

## What It Does

The planner combines:

- IMAP mailbox source planning,
- credential policy checking,
- human approval receipt verification.

It returns a `request_state` such as:

- `needs_human_approval`
- `ready_for_future_adapter_after_approval`
- `denied_by_human_decision`
- `denied_by_policy`
- `blocked`

## Important Safety Boundary

This release still does not implement live IMAP mailbox reads.

The new command does not:

- open an IMAP connection,
- attempt login,
- select a mailbox,
- read message headers,
- read message bodies,
- read attachments,
- create derived text,
- retrieve secrets,
- start OAuth,
- send email,
- delete email,
- change message flags,
- write files or receipts.

It also does not echo email addresses, username values, exact account refs,
exact credential refs, exact mailbox refs, IMAP host values, provider URLs,
message headers, message bodies, attachment names, approval receipt paths, local
absolute paths, tokens, or secret values.

## Migration

No archive migration is required.
