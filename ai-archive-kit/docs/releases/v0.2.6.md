# v0.2.6 README Baseline Display Patch

`v0.2.6` is a small public documentation patch.

It exists because `v0.2.5` correctly published the documentation map and philosophy documents, but the repository README baseline display needed to be carried forward as a separate non-destructive patch instead of moving an already-published tag.

## Changed

- Updated the English README current public baseline to `v0.2.6`.
- Updated the Korean README current public baseline to `v0.2.6`.
- Updated package metadata to `0.2.6`.
- Updated citation metadata to `0.2.6`.
- Updated versioning documentation to name `v0.2.6` as the current public showcase baseline.

## Migration

No private archive migration is required.

## Compatibility

Compatible with `v0.2.5`.

This patch does not change schemas, archive rules, zettel rules, or CLI behavior.

## Verification

```bash
cd ai-archive-kit
python -m unittest discover -s tests
python cli/archive.py doctor examples/fake-life-archive --strict
```

Expected result:

```text
tests pass
doctor reports 0 errors and 0 warnings
```

## Korean Note

`v0.2.6`은 README에 보이는 현재 공개 기준 버전을 정리하기 위한 작은 문서 패치입니다.

`v0.2.5` 태그를 강제로 옮기지 않고, 새 패치 버전으로 공개 기록을 이어갑니다.

private archive migration은 필요하지 않습니다.
