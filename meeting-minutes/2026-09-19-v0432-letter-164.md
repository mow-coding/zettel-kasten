# v0.4.32 — beta letter 164 first half (finalize scanner, probe kinds, Git backup attention, permission preview)

Date: 2026-09-19 (Korea Standard Time)

Executing model: Claude Opus 5, solo and sequential for implementation,
tests, docs, bump and release. Standing approval from the user to work
through the backlog. The user forwarded beta letter 164 with "read it and
start right away"; the client's letter folder was read and nothing else of
the client's was touched.

## Letter 164 triage

v0.4.32 ships ②, ⑤, ⑥ (the exposure half) and ⑦; v0.4.33 is planned for ①
(`object-storage-upload` under exact approval) with ③ and ④ inside that
design; v0.4.34 for ⑧ (delivered-feedback archival) and the 8b index
participation that ⑨ confirms. Reasons are in the decision log.

## Units (all synthetic fixtures)

1. Finalize scanner (②): byte-stream search with chunk overlap; 256 MiB
   ceiling; `oversize_skipped_count` / `_receipt_paths` and
   `unreadable_receipt_paths`; `scan_method: byte_stream_search`; the v0.4.30
   unreadable-receipt test rewritten for the new contract.
2. Permission preview (⑦): mode `permission_mode_preview`,
   `preview_permission_mode`, schema
   `wom-kit/work-session-permission-preview/v1`.
3. Probe failure kinds (⑤): thread-local failure kind in the capped runner,
   `probe_budget_exhausted` in the observation, per-probe `probes` list on
   the snapshot observation, `checks.git_transaction_snapshot.detail` in the
   update preflight.
4. Git backup attention (⑥): module `git_backup_attention`, attached to
   `ai-start-here`, `backup-evidence` (GitHub lane) and the `work-session`
   create / claim envelopes; Markdown section in the start-here map.

## Privacy note recorded the same day

The user asked whether local folder names had ever reached the public
repository. Current files: none (the readiness gate and release-docs tests
enforce it). History: two commits from 2026-07-30 carried the user's local
workspace path and the client's folder name until 2026-07-31 / 2026-08-29.
The user approved rewriting the public history; the rewrite is rehearsed on a
mirror (zero remaining matches, identical trees) and is executed after the
open pull requests merge, with its own minutes.

## Verification

`test_v0432_letter164` (15), the v0.4.30 claims module, the v0419
runtime-truth and component-failure modules, the capped-runner module, the
backup-evidence / start-here / work-session CLI cohort and the v0.4.30
edges module; the pin sweep and the full CI run on the candidate.
