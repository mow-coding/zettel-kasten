# v0.4.35 — beta letter 167 (updater vs the rewritten origin main)

Date: 2026-09-20 (Korea Standard Time)

Executing model: Claude Opus 5, solo and sequential. The user forwarded the
letter ("전달했는데 업데이트 또 안됐네") and asked whether the earlier
private-path exposure was resolved; both answers are in this record and the
decision log.

## What happened

The public-history rewrite of the morning (private identifiers purged from
every commit) made the client's source mirror unable to fast-forward
`origin/main`; the updater's non-forced atomic fetch refused, and the
result said only "fetch failed / tag missing". The client diagnosed it
correctly in the letter.

## Units (synthetic fixtures)

1. `_wom_kit_project_update_fetch_diagnosis`: ls-remote + ancestry check
   (objects left by the rejected fetch, else a removed probe ref).
2. `--affirm-origin-main-rewritten`: forced main refspec for the one fetch,
   `origin_main_rewrite_affirmed` bound warning, before/after ids.
3. Tests `test_v0435_letter167` (rewritten upstream refused with the codes,
   then accepted under the affirmation; diagnosis kinds).
4. Docs: decision log, this record, register row L167-01, release note,
   updater doc section, CHANGELOG/UPGRADE (client instruction), READMEs;
   bump.

## Exposure follow-up

The rewrite removed the identifiers from all history (0 matches after);
the GitHub cache purge request drafted that morning is the user's to send.

## CI correction (2026-09-20, PR #128 first run)

Both Ubuntu shards failed the approve-path test with
`terminal_cleanup_outcome_unknown` after the correctly diagnosed refusal. Reproduced in a
Linux container: the pre-existing stubbed fetch-failure CLI test gives the identical
status once its Windows-only skip is removed, because the approved updater preparation
is Windows CPython 3.12 only (every approve-path test in `test_cli` is skipped
elsewhere). The new test now carries the same `WINDOWS_PROJECT_RUNTIME` skip; the
diagnosis test runs on every platform. No product change.
