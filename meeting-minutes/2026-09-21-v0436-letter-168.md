# v0.4.36 — beta letter 168 (grants without dialogs, upload cause, delivered-letter archival)

Date: 2026-09-21 (Korea Standard Time)

Executing model: Claude Opus 5, solo and sequential; the upload root-cause
analysis ran as one read-only workflow of ten subagents.

## What the user said

The user forwarded letter 168 angry that problems the beta clients raised
repeatedly were still open, and — rightly — that he had decided on
2026-09-17 that the permission modes work like the desktop apps with no
dialog at all, had told the clients so, and the product still opened
dialogs. The implementer's exclusion list was never his decision; the
correction is recorded in the decision log.

## Units (synthetic fixtures)

1. U8 no-dialog grants: the always-dialog set is empty; the grant action is
   the only dialog-only action; allow_all dialog line names the scope.
2. U1 upload cause: broker allowlist, runner cause carry, CLI envelope,
   pre-dialog probes (index authority, credential resolvability),
   `--progress-log`, registered store labels.
3. U2 proven-no-effects failures close their claim
   (`exact_human_approval_writer_refused`).
4. U3 `operator-feedback-archive` (letters 164 ⑧ / 168 request 8).
5. U5 finalize fingerprint (no re-scan when unchanged).
6. U6 preview request keys; U7 compose default draft record + body ref.
7. Coverage manifest: environment route.
8. Docs: decision log, this record, register rows L168-01..05, release
   note, contract / capabilities / matrix / adapter-contract corrections,
   CHANGELOG / UPGRADE / READMEs; bump.

## Tests

`test_v0436_letter168` (16), the permission / broker / runner /
object-storage / claims cohorts, the release-docs cohort; CI on the
candidate PR.
