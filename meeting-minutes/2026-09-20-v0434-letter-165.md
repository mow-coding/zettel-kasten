# v0.4.34 — beta letter 165 (presenter-bound grants, legacy identifiers, compose under exact approval)

Date: 2026-09-20 (Korea Standard Time)

Executing model: Claude Opus 5, solo and sequential for design, implementation,
tests, docs, bump and release. A bounded read-only Ultracode workflow (8
agents, `wf_9b0ff2e8-8a9`) reviewed the first design; the corrected design
and three later deviations are recorded in the decision log. Standing approval
from the user to work through the backlog; the user asked to finish the
running work first and then take the new letter ("너 하던거 마저 하고 이것도
해주라"), and paused the session once for a shutdown; work resumed from the
local WIP commits.

## Scope decision

v0.4.34 takes the whole of letter 165: [A] presenter-bound, time-boxed session
grants with presenter evidence in the claims and operator visibility; [B] the
legacy identifier warning on every new-record surface; [C] the feedback body
write under exact approval; [D] the revise path announced; [E] revision-plan
warning explanations. The items previously planned for v0.4.34 (letter 164 ⑧
archival, 8b index participation, session refs for the always-dialog writers,
select-all) move to v0.4.35.

## Units (all synthetic fixtures)

1. `work_session_permission` v2 grant, `presenter_fingerprint`, registry
   validation, service minting, dispatcher `grant_hours`, dialog box line,
   MCP holder.
2. Claim key `session_presenter`, broker outcome codes and result evidence,
   claims listing counts, query rows and counts, `ai-start-here`
   `session_permission_attention` + Markdown + guidance, help text.
3. `legacy_identifier` module and its surfaces (create-draft with the
   `--allow-warnings` gate, mint, zet-revision-plan/write, objet-link,
   source-intake, ai-start-here guidance).
4. `operator_feedback_body_write` operation, compose routed through the
   broker, receipt v0.2 envelope, body-check flag, `--create-draft-record`,
   five-step next actions, reopened-writer set.
5. `zettel_quality_warning_explanations` / `zettel_markdown_table_evidence`.
6. Docs: decision log, this record, register row L165-01 and entry, release
   note, contract / ai-start-here / capabilities / concept-guide sentences,
   coverage row, release-docs test; bump (the AGENTS routing block is a
   versioned contract shared by the packaged templates and is left as is).

## Verification

`test_v0434_letter165` (21), `test_v0424_session_permission_modes` (8, updated
for the presenter token), `test_v0430_exact_approval_claims` (row pin moved),
the work-session, claims, feedback-body, objet-link and intake cohorts, the
inventory and coverage pins, the release-docs modules, the pin sweep and the
full CI run on the candidate.
